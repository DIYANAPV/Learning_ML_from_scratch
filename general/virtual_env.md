To create and activate a virtual environment:

1. Create it:

python3 -m venv .venv
2. Activate it:

source .venv/bin/activate
Your terminal prompt will then show (.venv) indicating it's active.

3. When you're done, deactivate with:

deactivate